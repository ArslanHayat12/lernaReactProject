{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "dependencies": {
    "@lerna-react-modules/components": "^0.2.0",
    "@types/node": "^12.0.4",
    "save-dev": "^2.0.0",
    "typescript": "^3.5.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "start": "lerna run start --stream",
    "tsc": "lerna run tsc --stream",
    "watch": "lerna run watch --stream",
    "publish": "lerna run tsc && lerna publish"
  }
}
